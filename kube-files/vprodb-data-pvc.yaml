apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: vprodb
  name: vpro-db-pvc
  namespace: testy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: local-storage
  volumeMode: Filesystem
  volumeName: vprodb-pv
